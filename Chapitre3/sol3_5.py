# Ecrivez juste la fonction rac_eq_2nd_deg()

# Attention si delta > 0 : solutions = [racine_1, racine_2]
# ou racine_1 c'est avec plus (+) V(delta); racine_2 c'st avec moins (-) V(delta)

import math

def rac_eq_2nd_deg(*coeff):
	pass